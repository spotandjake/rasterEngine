// PORT: Make this a struct when converting to c#
export default class Vec3 {
  // Internals
  // Externals
  // PORT: Make these float64 when converting to c#
  public X: number;
  public Y: number;
  public Z: number;
  // Constructor
  // PORT: Make these float64 when converting to c#
  constructor(x: number, y: number, z: number) {
    this.X = x;
    this.Y = y;
    this.Z = z;
  }
  // Methods
}
